# Знакомство с find

`find` ищет файлы и директории по критериям. Общий вид:

    find <где> <критерии>

Например:

    find /home/user -name "*.txt"
    find . -maxdepth 4 -name "*.txt"

Обратите внимание: у `find` **длинные** ключи всё равно пишутся с одним
дефисом (`-maxdepth`, а не `--maxdepth`).

Несколько условий комбинируются как «И», а `-o` даёт «ИЛИ».

## Задание

Найдите всё, что лежит в `/home/student/search`, **не глубже 3-го уровня
вложенности**.

Нажмите **Enter**.
