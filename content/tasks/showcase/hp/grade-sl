#!/bin/sh
# Hidden grader for the sl stage. Runs with the student's filesystem at /. Accept (exit 0) iff the
# `sl` package is installed (the student ran sudo apt-get install -y sl). Invisible to the student.
command -v sl >/dev/null 2>&1 && exit 0
[ -x /usr/games/sl ] && exit 0
exit 1
