- Очень интересно работает монтирование overlay. Если смонтировать последним слой, в котором добавлен новый пользователь, и запустить через ./make_env.py create, то тогда войти в пользователя будет невозможно, но если добавить еще один пустой слой в конце, то все становится на свои места :)


