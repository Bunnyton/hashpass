image:
```
bunnyton/help:latest
```

readme.txt:
```

Когда много ключей их можно объединять, например:
ls -lat
ls -alt
ls -tla
ls -l -a -t
ls -l -at
....
Все эти команды делают одно и то же.

Как можно было заметить, довольно часто ключи - это одна буква, однако ключами могут становиться и слова. Например:
ls --help

#Задание
Посмотрите, что выведет эта команда
```

action: 
```
action echo -e "\n\nМожешь пролистать глазами, а ключ будет через 45 секунд"
```
```
action sleep 45
```

