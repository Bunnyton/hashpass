services:
  hashpass_server:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - ./storage:/app/storage
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro

