# Fast, reliable shutdown for the throwaway task container. `exit` powers the machine off; if any
# service is slow/stuck to stop, the default 90s TimeoutStopSec makes `hashpass run` appear to hang
# on exit ("kills the terminal"). Cap it so poweroff completes in seconds.
[Manager]
DefaultTimeoutStopSec=8s
DefaultTimeoutStartSec=25s
